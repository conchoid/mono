# Docker Images for mono

This repository contains Dockerfiles to install mono within docker containers. Pre-build images are available at [Docker Hub](https://hub.docker.com/repository/docker/conchoid/mono/).
